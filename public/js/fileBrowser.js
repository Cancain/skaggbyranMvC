const rootEl = document.getElementById('fileBrowserContainer');
const spanElement = React.createElement('span', {}, 'SPAN!');

import Test from './Test';

const element = <Test></Test>;
ReactDOM.render(element, rootEl);